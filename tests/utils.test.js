// const parseConfig=require('../src/utils/parseConfig');

describe('utils test', () => {
  test('simple method', async () => {
    // console.log(parseConfig([{
    //   url:'/api',
    //   callback:function(){}
    // }]));
    // expect(res.text).toBe('test success');
  });
});

//what't more, next write